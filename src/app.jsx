// import {loadMetaModel} from "./services/metaModel";
//
// export function modifyRouteProps(props, { route }) {
//   const metaModel = {};
//   if (route && route.name) {
//     loadMetaModel(route.name).then(meta => {
//       if (meta && meta.metaModelList && meta.metaModelList.length > 0) {
//         metaModel.metaModelList = meta.metaModelList;
//       }
//     });
//   }
//   return { ...props, metaModel };
// }
